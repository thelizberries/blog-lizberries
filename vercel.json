{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "UPLOAD_PASSWORD": "@upload-password",
    "GITHUB_TOKEN": "@github-token"
  }
}
